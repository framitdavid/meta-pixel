export enum GenericLogMessage {
  WindowNotAvailable = 'Cannot initialize js-meta-pixel without window available',
  PixelNotInitialized = 'Please initialize Pixel by calling Pixel.initialize(your-pixel-id: string) before start tracking.',
}
