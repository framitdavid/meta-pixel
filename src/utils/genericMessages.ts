export enum GenericLogMessage {
  WindowNotAvailable = 'Cannot initialize js-meta-pixel without window available',
  PixelNotInitialized = 'Please initialize Pixel by calling Pixel.initialize({ pixelId: "your-pixel-id" }) before start tracking.',
}
